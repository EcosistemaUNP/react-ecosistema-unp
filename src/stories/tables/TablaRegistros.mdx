import { Meta, Story, Canvas, Stories } from '@storybook/blocks';
import TablaRegistrosStories from './TablaRegistros.stories';

<Meta of={TablaRegistrosStories} />

# TablaRegistros
El componente `TablaRegistros` es una tabla para la visualización de registros.

## Ejemplo de uso

- MiComponente.tsx
```tsx
import { TablaRegistros } from 'react-ecosistema-unp/tables';

const MiComponente: React.FC = () => {
  return (
    <TablaRegistros />
  );
}
```

## Previsualización

<Stories />


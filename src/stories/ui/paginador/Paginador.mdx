import { Meta, Story, Canvas, Stories, ArgTypes, Controls, Markdown } from '@storybook/blocks';
import * as PaginadorStories from './Paginador.stories';
// import Ventana from '../Ventana.md?raw'

<Meta of={PaginadorStories} />

{/* <Markdown>{ Ventana }</Markdown> */}

# VentanaLienzo
El componente `VentanaLienzo` es un contenedor en blanco. Incluye el menú lateral y una barra superior para el breadcrumb.

## Ejemplo de uso

- MiComponente.tsx
```tsx

```

## Props

<ArgTypes />

## Previsualización

<Stories />

